version: '3.8'

services:
  scheduling-service:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/scheduling.db
    restart: unless-stopped
